window.QuickTaskContract = Object.freeze({
  visibleZoneCount: 6,
  durations: Object.freeze([5, 15, 30, 60]),
  defaultDuration: 15,
  context: "Runs once, right now",
  precondition: "Pick at least one zone to start · durations in minutes",
});
